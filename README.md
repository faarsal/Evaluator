# Evaluator

### concat.py ->concatinate all .cpp files in folder and merge them into a single file eliminating main function
### checkAll.py ->check All folders in a directory and update marks
### test.cpp -> All containing all test functions
### mainGenerator -> Generate main() for test.cpp
